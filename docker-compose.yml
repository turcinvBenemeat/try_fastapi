# version: '3'
services:
  ingeborge-statistics:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8002:80"
    command: "uvicorn main:app --host 0.0.0.0 --port 80"
    env_file: ".env"
    restart: "always"
    volumes:
      - ./logs:/app/logs
