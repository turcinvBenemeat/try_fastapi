"""
SQLAlchemy ORM models.

Each module defines one or more tables mapped to ``Base`` from ``src.database``.
Import concrete models (e.g. ``Task``, ``Project``) where you need table metadata
or relationships—not this package alone.
"""
