# VCS
.git
.gitignore

# Secrets (do not bake real credentials into the image)
.env

# Python
__pycache__
*.py[cod]
*$py.class
.pytest_cache
.mypy_cache
.ruff_cache
.venv
venv
*.egg-info
dist
build

# IDE / OS
.idea
.vscode
.DS_Store

# Runtime / local data
logs
*.db

# Docs, compose, and build metadata (not needed at runtime)
*.md
docker-compose*.yml
Dockerfile
.dockerignore

# Tests are mounted in compose; omit from production layers
tests/
